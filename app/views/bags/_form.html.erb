<%= form_with(model: bag, local: true) do |form| %>
  <% if bag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bag.errors.count, "error") %> prohibited this bag from being saved:</h2>

      <ul>
      <% bag.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :quantity %>
    <%= form.number_field :quantity %>
  </div>

  <div class="field">
    <%= form.label :product_id %>
    <%= form.text_field :product_id %>
  </div>

  <div class="field">
    <%= form.label :product_variant_id %>
    <%= form.text_field :product_variant_id%>
  </div>

  <div class="field">
    <%= form.label :purchased %>
    <%= form.text_field :purchased %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
